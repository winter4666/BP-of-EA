spring:
   datasource:
      url: jdbc:mysql://localhost/demo
      username: root
      password: secret
   flyway:
      cleanDisabled: false

logging:
   level:
      org.hibernate.orm.jdbc.bind: trace
      org.hibernate.SQL: debug